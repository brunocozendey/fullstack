"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _default = Query => class extends Query {
  _limitString() {
    let query = '';

    if (this._limit !== void 0) {
      query = `FIRST ${this._limit}`;
    }

    if (this._offset !== void 0) {
      if (query !== '') {
        query += ' ';
      }

      query += `SKIP ${this._offset}`;
    }

    return {
      query,
      position: 'before'
    };
  }

  escapeField(arg) {
    return arg;
  }

  _getSymbolParam() {
    return '?';
  }

  toQuery() {
    const query = this.toSQL();
    let index = -1;
    return query.sql.replace(/\?+/g, char => {
      index++;
      return query.bindings[index];
    });
  }

};

exports.default = _default;